# OpenShift Installation Scripts

This repository contains automation scripts for setting up OpenShift 4 clusters using different installation methods.
Currently it includes a bastion setup script for the **agent-based connected** method that automates DNS (BIND) and HAProxy configuration on a bastion host.
More installation methods (UPI, IPI, Disconnected, SNO) will be added over time.
